# 固定区域 5km 累计降水与雨雪相态网格编辑及复盘系统建设方案

**版本**：V1.0  
**日期**：2026-05-14  
**适用范围**：固定区域 5km 网格、24h/48h/168h 累计降水与相态产品订正、EC IFS 多要素复盘绘图  
**方案目标**：建设一个轻量、可落地、可追溯、可复盘的网格编辑系统。系统只对累计降水和相态进行交互订正；EC IFS 的高空高度场、风、温度、湿度及其他变量不进入主编辑流程，而作为复盘绘图和后续分析的数据输入。

---

## 1. 项目背景

当前已有基础条件包括：

1. 固定区域底图，包括 PNG 底图以及绘制该底图的代码。
2. 基于 EC IFS 预报数据加密得到的 5km 格点预报。
3. 5km 分辨率 DEM 数据。
4. 已有基于预报数据进行相态调整并出图的结果图。
5. 后续将提供复盘绘图代码和测试数据，包括 EC IFS 多层次、多变量诊断场，例如 850hPa、700hPa、500hPa 等高度上的高度场、风、温度、湿度及其他变量。

本项目不建设综合性气象网格平台，也不建设多模式、多要素交互浏览系统。建设重点是：

> 在固定区域 5km 网格上，对 24h、48h、168h 可控累计时段的降水量和相态结果进行人工订正，并把订正前后格点场、差异场、修改掩膜、操作日志、基础出图和 IFS 多要素复盘组合图完整保存下来。

---

## 2. 对陕西 GFIE 手册的吸收与取舍

陕西省格点预报智能编辑系统 GFIE 的用户手册中，系统具有编辑工具栏、图例栏、背景数据选择、时间选择、模块菜单、要素菜单、通用工具栏、图层属性菜单栏、状态栏等组成部分。其编辑工具包括主客观融合、落区增加、权重修改、多边形、绘制笔、圆形、笔刷、区域移动、区域复制、筛查、要素协同等。

本项目不照搬 GFIE 的完整平台形态，而是吸收其中适合本项目边界的编辑思想。

### 2.1 建议吸收的 GFIE 功能思想

| GFIE 功能思想 | 在本系统中的转化 |
|---|---|
| 时间选择 | 转化为 24h/48h/168h 累计长度与起止时效选择 |
| 多边形 | 用于大范围圈选并修改累计降水或相态 |
| 绘制笔 | 用于沿雨雪边界、降水边界进行不规则区域修正 |
| 笔刷 | 用于小范围修补量级、相态、孤立斑块 |
| 筛查 | 转化为弱降水清零、选区内相态批处理、选区内量级统一 |
| 撤销、恢复、保存 | 转化为操作级撤销、重做、版本保存和复盘归档 |
| 状态栏显示格点值 | 转化为当前鼠标格点诊断栏，只显示 qpf、ptype、修改状态等核心信息 |
| 区域移动/复制 | 作为二期功能，用于落区整体偏移或复制 |

### 2.2 不建议纳入首版的 GFIE 功能

| GFIE 功能 | 不纳入原因 |
|---|---|
| 背景数据选择 | 本项目没有多背景场选择需求，输入产品固定 |
| 多要素交互显示 | IFS 多要素只用于复盘绘图，不进入主编辑界面 |
| 格点趋势调整 | 本项目只编辑累计结果，不拆分到 3h/6h 分段，不做时间趋势回推 |
| 要素协同 | 首版只编辑降水和相态，不做多要素协同修改 |
| 站点修改 | 本项目编辑对象是格点产品，不是站点产品 |
| 预警信号制作 | 不属于本项目建设目标 |
| 短临预警制作 | 不属于本项目建设目标 |
| 大量独立工具按钮 | 容易造成操作复杂，建议改为任务式工具面板 |

---

## 3. 建设目标

### 3.1 总体目标

建设一个固定区域、固定 5km 网格的累计降水与雨雪相态网格编辑系统。系统支持用户选择累计时段，对降水量级和相态进行区域化人工订正，自动生成订正前后对比、差异图、相态变化图、修改掩膜、操作日志及 IFS 多要素复盘组合图。

### 3.2 核心目标

1. **可编辑**：支持对累计降水量和相态进行多边形、绘制笔、笔刷等交互式订正。
2. **可控时段**：支持 24h、48h、168h 累计长度，并允许控制起始时效和结束时效。
3. **可出图**：复用已有底图和绘图代码，生成订正前、订正后、差异、相态变化、复盘组合图。
4. **可追溯**：保存每一步操作，包括操作人、时间、工具、范围、参数、影响格点数、订正前后统计。
5. **可复盘**：保存订正前后格点数据、IFS 多要素诊断场切片、绘图输入清单、绘图配置和复盘结果图。
6. **可扩展**：后续可接入更多 IFS 变量、更多复盘模板、实况检验和批量个例分析。

---

## 4. 建设边界

### 4.1 本期纳入范围

1. 固定区域 5km 网格。
2. 固定 PNG 底图显示。
3. 24h、48h、168h 累计降水产品。
4. 累计时段综合相态产品。
5. 降水量级编辑。
6. 雨雪相态编辑。
7. 多边形、绘制笔、笔刷选区。
8. 弱降水筛查清零。
9. 撤销、重做、保存版本。
10. 订正前后结果图生成。
11. 降水差异图、相态变化图、人工修改区域图生成。
12. 当前时段 + 前后 24h 的复盘组合绘图。
13. EC IFS 多要素复盘变量归档与绘图接口。
14. 操作日志和复盘数据包保存。

### 4.2 本期不纳入范围

1. 不做多背景场选择。
2. 不做多模式切换。
3. 不把 850/700/500 等 IFS 要素做成前端交互浏览图层。
4. 不做 3h/6h 分段拆分。
5. 不把修改后的累计量回推到分段原始数据。
6. 不做温度、湿度、风场等要素的人工编辑。
7. 不做站点编辑。
8. 不做预警信号制作。
9. 不做短临预警制作。
10. 不做 AI 自动订正。

---

## 5. 系统总体架构

```text
输入数据
├── 固定区域底图 PNG
├── 固定区域 5km 网格定义
├── 累计降水场 qpf：24h / 48h / 168h
├── 累计相态场 ptype
└── EC IFS 复盘诊断场
    ├── 500hPa / 700hPa / 850hPa 高度场
    ├── 500hPa / 700hPa / 850hPa 风场
    ├── 500hPa / 700hPa / 850hPa 温度场
    ├── 500hPa / 700hPa / 850hPa 湿度场
    └── 其他变量

前端编辑界面
├── 产品窗口选择
├── 固定底图显示
├── 当前累计降水/相态显示
├── 多边形/绘制笔/笔刷编辑
├── 降水量调整
├── 相态调整
├── 筛查清零
├── 预览、应用、撤销、重做
└── 保存版本、生成复盘

后端服务
├── 产品窗口管理
├── 格点数据加载
├── 选区 mask 生成
├── 降水/相态编辑引擎
├── 差异场和修改掩膜生成
├── 操作日志记录
├── 版本保存
├── 基础出图调用
└── 复盘绘图任务调度

复盘系统
├── IFS 复盘变量切片
├── 当前时段 + 前后24h 数据整理
├── plot_input_manifest.json 生成
├── 调用用户提供的绘图代码
├── 保存复盘图
└── 归档复盘数据包
```

---

## 6. 业务流程

### 6.1 编辑流程

```text
1. 用户选择起报时间
2. 用户选择累计长度：24h / 48h / 168h
3. 用户选择起始时效
4. 系统自动计算结束时效
5. 系统加载 qpf_before、ptype_before
6. 系统初始化 qpf_after = qpf_before，ptype_after = ptype_before
7. 用户选择多边形、绘制笔或笔刷
8. 用户选择编辑动作：降水定值、增加、减少、乘系数、清零，或相态改雨/雨夹雪/雪/无降水
9. 系统生成预览
10. 用户确认应用
11. 系统记录操作日志
12. 用户保存版本
13. 系统生成差异场、相态变化、修改掩膜
14. 系统调用基础绘图代码
15. 系统调用复盘绘图代码
16. 系统保存复盘数据包
```

### 6.2 复盘流程

```text
1. 接收已保存的 edit_version
2. 定位当前累计时段
3. 计算前 24h 与后 24h 窗口
4. 读取当前窗口 qpf/ptype 订正前后数据
5. 读取前后 24h 的 qpf/ptype 数据
6. 读取对应 IFS 复盘变量
7. 生成 plot_input_manifest.json
8. 调用复盘绘图代码
9. 保存 review_composite.png 及其他复盘图
10. 记录复盘产品信息
```

---

## 7. 产品窗口设计

### 7.1 累计时段控制

系统只支持三种累计长度：

```text
24h
48h
168h
```

累计时段由两个参数控制：

```text
accum_hours：累计长度，取值 24 / 48 / 168
start_lead：起始预报时效
end_lead：结束预报时效，end_lead = start_lead + accum_hours
```

示例：

| 累计长度 | 起始时效 | 结束时效 | 窗口含义 |
|---:|---:|---:|---|
| 24h | +000h | +024h | 起报后 0 至 24 小时累计降水 |
| 24h | +024h | +048h | 起报后 24 至 48 小时累计降水 |
| 48h | +000h | +048h | 起报后 0 至 48 小时累计降水 |
| 48h | +048h | +096h | 起报后 48 至 96 小时累计降水 |
| 168h | +000h | +168h | 起报后 0 至 168 小时累计降水 |

### 7.2 窗口 ID 命名

建议使用统一命名规则：

```text
window_id = {init_time}_ACC{accum_hours}_{start_lead}_{end_lead}
```

示例：

```text
2026010108_ACC24_024_048
2026010108_ACC48_000_048
2026010108_ACC168_000_168
```

---

## 8. 数据对象设计

### 8.1 编辑主变量

| 变量 | 含义 | 是否可编辑 |
|---|---|---:|
| qpf_before | 订正前累计降水 | 否 |
| ptype_before | 订正前累计相态 | 否 |
| qpf_after | 订正后累计降水 | 是 |
| ptype_after | 订正后累计相态 | 是 |

### 8.2 复盘派生变量

| 变量 | 含义 |
|---|---|
| delta_qpf | qpf_after - qpf_before |
| change_ptype | ptype_before 到 ptype_after 的变化编码 |
| edit_mask | 是否被人工修改 |
| operation_mask | 单次操作影响范围 |

### 8.3 IFS 复盘变量

IFS 变量不参与交互编辑，只参与复盘绘图和归档。变量清单以后按实际测试数据确定。建议预留如下结构：

| 类别 | 变量示例 | 层次示例 |
|---|---|---|
| 高度场 | z / gh / hgt | 500 / 700 / 850 hPa |
| 风场 | u / v / wind_speed / wind_dir | 500 / 700 / 850 hPa |
| 温度 | t | 500 / 700 / 850 hPa |
| 湿度 | rh / q | 500 / 700 / 850 hPa |
| 地面或近地面 | mslp / t2m / d2m / total_precip | 按数据实际情况 |
| 其他变量 | cape / cin / pwat / freezing_level 等 | 按数据实际情况 |

---

## 9. 相态编码建议

相态场建议采用整数编码，前端显示中文含义，后台保存编码。

| 编码 | 含义 |
|---:|---|
| 0 | 无降水 |
| 1 | 雨 |
| 2 | 雨夹雪 / 混合相态 |
| 3 | 雪 |
| 4 | 冻雨，可选 |
| 5 | 冰粒，可选 |

首版建议只启用：

```text
0 = 无降水
1 = 雨
2 = 雨夹雪 / 混合相态
3 = 雪
```

如果现有出图代码已经有固定相态编码，应优先沿用现有编码，但需要在系统配置中明确映射关系。

---

## 10. 前端界面设计

### 10.1 顶部：产品窗口选择区

```text
起报时间：YYYY-MM-DD HH
累计长度：24h / 48h / 168h
起始时效：+000h / +024h / +048h / ...
结束时效：自动计算
当前版本：原始 / 草稿 / 已保存版本
按钮：预览 / 应用 / 撤销 / 重做 / 保存 / 生成复盘 / 导出数据包
```

### 10.2 左侧：查看内容

```text
原始结果图
当前订正图
降水差异图
相态变化图
人工修改区域图
复盘组合图
```

首版不设置 850hPa、700hPa、500hPa 等交互图层开关。这些变量只进入复盘绘图任务。

### 10.3 中间：地图编辑区

```text
固定 PNG 底图
累计降水/相态色斑图
选区边界
笔刷范围
编辑预览效果
当前鼠标格点提示
```

### 10.4 右侧：编辑工具面板

分为四个页签：

```text
区域选择
降水调整
相态调整
操作历史
```

#### 区域选择

```text
多边形
绘制笔
笔刷
圆形，二期可选
清除选区
反选，二期可选
```

#### 降水调整

```text
定值：qpf = value
增加：qpf += delta
减少：qpf -= delta，最小为 0
乘系数：qpf *= factor
清零：qpf = 0，ptype = 0
弱降水筛查：qpf <= threshold 时清零
```

#### 相态调整

```text
设为无降水
设为雨
设为雨夹雪
设为雪
恢复原始相态
```

#### 操作历史

```text
操作时间
工具类型
变量
动作
影响格点数
影响面积
撤销/重做入口
```

---

## 11. 编辑功能详细设计

### 11.1 多边形编辑

适用场景：

```text
大范围降水量级增强或减弱
大范围相态从雨改雪
大范围清零弱降水
```

交互流程：

```text
1. 点击多边形工具
2. 左键添加顶点
3. 双击完成绘制
4. 右侧选择操作类型和参数
5. 点击预览
6. 确认后点击应用
```

不建议使用“画完即变”的模式，所有操作均需先预览再应用。

### 11.2 绘制笔编辑

适用场景：

```text
沿雨雪边界修正相态
沿山脉或降水边界画不规则狭长区域
修补不规则降水落区
```

绘制笔生成的是一个带宽度的线状缓冲区，缓冲区内格点进入 mask。

### 11.3 笔刷编辑

适用场景：

```text
擦除孤立雪点
补充局地雪区
局地增加或减少降水量
小范围相态修正
```

笔刷参数：

```text
半径：1 / 2 / 3 / 5 个格点
模式：硬边界 / 软边界，首版可只做硬边界
动作：降水增加/减少/定值/清零，或相态设置
快捷操作：Ctrl + 鼠标滚轮调整笔刷大小
```

### 11.4 降水编辑算法

```python
# mask 为当前选区布尔数组

# 定值
qpf_after[mask] = value

# 增加
qpf_after[mask] = qpf_after[mask] + delta

# 减少
qpf_after[mask] = np.maximum(qpf_after[mask] - delta, 0)

# 乘系数
qpf_after[mask] = qpf_after[mask] * factor

# 清零
qpf_after[mask] = 0
ptype_after[mask] = 0
```

### 11.5 相态编辑算法

```python
# qpf_threshold 建议默认 0.1mm，可配置
valid_precip = qpf_after > qpf_threshold

# 设置相态
ptype_after[mask & valid_precip] = target_ptype

# 无降水区域强制置为 0
ptype_after[~valid_precip] = 0
```

### 11.6 筛查清零

```python
screen_mask = selected_mask & (qpf_after <= threshold)
qpf_after[screen_mask] = 0
ptype_after[screen_mask] = 0
```

筛查阈值默认：

```text
0.1 mm
```

可根据业务配置为：

```text
0.1 mm / 0.5 mm / 1.0 mm / 自定义
```

---

## 12. 一致性规则

首版只做最基础、必要的一致性规则。

### 12.1 硬规则

| 规则 | 处理方式 |
|---|---|
| qpf <= threshold 时，ptype 必须为无降水 | 自动修正 |
| ptype = 无降水但 qpf > threshold | 提示用户，允许选择保留降水或清零 |
| qpf_after 小于 0 | 自动修正为 0 |

### 12.2 软提示

| 规则 | 处理方式 |
|---|---|
| 大面积相态被修改 | 保存时提示影响格点数和面积 |
| 降水总量变化过大 | 保存时提示订正前后总量差异 |
| 相态变化跨类别过大，例如雨直接改雪 | 记录到操作日志，不阻止保存 |

不引入温度、湿度、地形等一致性校验，避免系统超出当前数据编辑边界。

---

## 13. 版本与留痕设计

### 13.1 版本类型

```text
v0_original：原始结果，不可修改
v1_draft：当前草稿，可反复编辑
vN_saved：保存版本，可用于复盘和归档
```

每次保存生成新的 `version_id`。

### 13.2 保存内容

每个保存版本至少包含：

```text
qpf_before
ptype_before
qpf_after
ptype_after
delta_qpf
change_ptype
edit_mask
operation_log
before_image
after_image
delta_qpf_image
change_ptype_image
edit_mask_image
review_composite_image
plot_input_manifest
```

### 13.3 操作日志字段

```json
{
  "operation_id": "op_000023",
  "case_id": "2026010108",
  "window_id": "2026010108_ACC24_024_048",
  "version_id": "v003",
  "user": "forecaster_001",
  "created_at": "2026-01-01T10:15:32",
  "tool": "polygon",
  "variable": "qpf",
  "operation": "increase",
  "parameters": {
    "delta_mm": 3.0,
    "only_nonzero": true
  },
  "affected_grid_count": 286,
  "affected_area_km2": 7150,
  "before_stats": {
    "min": 0.2,
    "max": 8.5,
    "mean": 3.1,
    "sum": 886.6
  },
  "after_stats": {
    "min": 3.2,
    "max": 11.5,
    "mean": 6.1,
    "sum": 1744.6
  }
}
```

相态修改日志示例：

```json
{
  "operation_id": "op_000024",
  "case_id": "2026010108",
  "window_id": "2026010108_ACC24_024_048",
  "version_id": "v004",
  "user": "forecaster_001",
  "created_at": "2026-01-01T10:21:10",
  "tool": "brush",
  "variable": "ptype",
  "operation": "set_ptype",
  "parameters": {
    "target_ptype": "snow"
  },
  "affected_grid_count": 91,
  "ptype_transition": {
    "rain_to_snow": 52,
    "rain_snow_to_snow": 39
  }
}
```

---

## 14. 复盘系统设计

### 14.1 复盘系统定位

复盘系统不参与人工编辑，只负责把编辑结果和 IFS 多要素诊断场组织成绘图输入，并调用用户提供的绘图代码生成组合图。

### 14.2 当前时段与前后 24h 定义

当前编辑窗口：

```text
current_window = [start_lead, end_lead]
```

前 24h：

```text
prev24_window = [start_lead - 24, start_lead]
```

后 24h：

```text
next24_window = [end_lead, end_lead + 24]
```

示例：

```text
当前窗口：+024h ~ +048h，24h累计
前24h：+000h ~ +024h
后24h：+048h ~ +072h
```

如果前后 24h 数据不存在，则复盘图对应区域标注“无数据”，并在 manifest 中记录缺失。

### 14.3 IFS 诊断场时间取样

累计降水是一个时间段，而高度场、风场、温度场、湿度场通常是某个有效时刻的预报场。因此需要制定时间取样策略。

建议预留配置：

```json
{
  "review_time_policy": {
    "current_window": ["start", "middle", "end"],
    "prev24_window": ["end"],
    "next24_window": ["start"],
    "default_pressure_field_time": "middle"
  }
}
```

首版可简化为：

```text
当前窗口：取中间时刻和结束时刻
前24h：取结束时刻
后24h：取开始时刻
```

具体取样规则可在接入绘图代码后再精确调整。

### 14.4 复盘绘图输入结构

建议后端整理成 `review_payload`：

```python
review_payload = {
    "metadata": {
        "case_id": "2026010108",
        "region_id": "fixed_region",
        "grid_id": "region_5km_v1",
        "init_time": "2026-01-01T08:00:00",
        "accum_hours": 24,
        "start_lead": 24,
        "end_lead": 48,
        "version_id": "v003",
        "title": "2026-01-01 08起报 +024~+048h 24h累计降水订正复盘"
    },
    "edit_fields": {
        "qpf_before": "edit_fields/qpf_before.nc",
        "ptype_before": "edit_fields/ptype_before.nc",
        "qpf_after": "edit_fields/qpf_after.nc",
        "ptype_after": "edit_fields/ptype_after.nc",
        "delta_qpf": "edit_fields/delta_qpf.nc",
        "change_ptype": "edit_fields/change_ptype.nc",
        "edit_mask": "edit_fields/edit_mask.nc"
    },
    "review_windows": {
        "prev24": {
            "start_lead": 0,
            "end_lead": 24,
            "qpf": "review_windows/prev24_qpf.nc",
            "ptype": "review_windows/prev24_ptype.nc"
        },
        "current": {
            "start_lead": 24,
            "end_lead": 48,
            "qpf_before": "edit_fields/qpf_before.nc",
            "ptype_before": "edit_fields/ptype_before.nc",
            "qpf_after": "edit_fields/qpf_after.nc",
            "ptype_after": "edit_fields/ptype_after.nc"
        },
        "next24": {
            "start_lead": 48,
            "end_lead": 72,
            "qpf": "review_windows/next24_qpf.nc",
            "ptype": "review_windows/next24_ptype.nc"
        }
    },
    "ifs_fields": [
        {
            "name": "z",
            "level_type": "pressure",
            "level_value": 500,
            "lead_hour": 48,
            "path": "ifs_review_fields/z_500_048.nc"
        },
        {
            "name": "t",
            "level_type": "pressure",
            "level_value": 850,
            "lead_hour": 48,
            "path": "ifs_review_fields/t_850_048.nc"
        },
        {
            "name": "u",
            "level_type": "pressure",
            "level_value": 850,
            "lead_hour": 48,
            "path": "ifs_review_fields/u_850_048.nc"
        },
        {
            "name": "v",
            "level_type": "pressure",
            "level_value": 850,
            "lead_hour": 48,
            "path": "ifs_review_fields/v_850_048.nc"
        },
        {
            "name": "rh",
            "level_type": "pressure",
            "level_value": 700,
            "lead_hour": 48,
            "path": "ifs_review_fields/rh_700_048.nc"
        }
    ],
    "output": {
        "review_composite": "images/review_composite.png"
    }
}
```

### 14.5 绘图模板管理

建议后续支持多个复盘模板：

| 模板 ID | 模板名称 | 内容 |
|---|---|---|
| precip_phase_compare_v1 | 降水相态订正对比图 | 订正前、订正后、降水差异、相态变化 |
| window_prev_next_v1 | 当前时段与前后 24h 图 | 前24h、当前、后24h |
| synoptic_500_700_850_v1 | 高空环流诊断图 | 500/700/850 高度、风、温湿 |
| snow_phase_review_v1 | 雨雪相态复盘图 | 编辑结果 + IFS 诊断场 |
| full_review_v1 | 综合复盘大图 | 订正对比 + 前后24h + 高空场 + 操作统计 |

绘图模板配置示例：

```json
{
  "template_id": "snow_phase_review_v1",
  "template_name": "雨雪相态订正复盘图",
  "required_fields": [
    "qpf_before",
    "ptype_before",
    "qpf_after",
    "ptype_after",
    "delta_qpf",
    "change_ptype",
    "z500",
    "t850",
    "rh700",
    "u850",
    "v850"
  ],
  "plot_function": "plot_snow_phase_review"
}
```

---

## 15. 数据归档目录结构

建议每个产品窗口形成独立目录。

```text
case_2026010108/
    window_ACC24_024_048/
        metadata.json

        edit_fields/
            qpf_before.nc
            ptype_before.nc
            qpf_after.nc
            ptype_after.nc
            delta_qpf.nc
            change_ptype.nc
            edit_mask.nc

        review_windows/
            prev24_qpf.nc
            prev24_ptype.nc
            next24_qpf.nc
            next24_ptype.nc

        ifs_review_fields/
            manifest.json
            z_500_048.nc
            z_700_048.nc
            z_850_048.nc
            t_500_048.nc
            t_700_048.nc
            t_850_048.nc
            rh_500_048.nc
            rh_700_048.nc
            rh_850_048.nc
            u_500_048.nc
            v_500_048.nc
            u_700_048.nc
            v_700_048.nc
            u_850_048.nc
            v_850_048.nc

        operations/
            operation_log.jsonl

        stats/
            qpf_stats.csv
            ptype_change_matrix.csv
            edit_summary.csv

        images/
            before_product.png
            after_product.png
            delta_qpf.png
            change_ptype.png
            edit_mask.png
            review_composite.png
            review_500_700_850.png
            review_full.png

        plot/
            plot_config.json
            plot_input_manifest.json
            plot_code_version.txt
            plot_log.txt
```

### 15.1 数据格式建议

| 数据类型 | 建议格式 | 说明 |
|---|---|---|
| 小规模原型 | `.npy` / `.npz` | 开发快，便于测试 |
| 业务归档 | NetCDF | 便于保存维度、变量、单位和元数据 |
| 大规模复盘样本库 | Zarr | 便于多时效、多版本、局部读取 |
| 图片产品 | PNG | 便于展示和归档 |
| 操作日志 | JSONL | 一行一个操作，便于追加和分析 |
| 统计结果 | CSV | 便于业务人员查看 |

首版建议：

```text
原型阶段：NPZ + PNG + JSONL + CSV
业务归档阶段：NetCDF + PNG + JSONL + CSV
```

---

## 16. 数据库设计

### 16.1 forecast_case

```sql
CREATE TABLE forecast_case (
    case_id TEXT PRIMARY KEY,
    init_time TEXT NOT NULL,
    region_id TEXT NOT NULL,
    grid_id TEXT NOT NULL,
    created_at TEXT NOT NULL
);
```

### 16.2 product_window

```sql
CREATE TABLE product_window (
    window_id TEXT PRIMARY KEY,
    case_id TEXT NOT NULL,
    accum_hours INTEGER NOT NULL,
    start_lead INTEGER NOT NULL,
    end_lead INTEGER NOT NULL,
    qpf_before_path TEXT NOT NULL,
    ptype_before_path TEXT NOT NULL,
    created_at TEXT NOT NULL
);
```

### 16.3 edit_version

```sql
CREATE TABLE edit_version (
    version_id TEXT PRIMARY KEY,
    window_id TEXT NOT NULL,
    version_no INTEGER NOT NULL,
    qpf_after_path TEXT NOT NULL,
    ptype_after_path TEXT NOT NULL,
    delta_qpf_path TEXT NOT NULL,
    change_ptype_path TEXT NOT NULL,
    edit_mask_path TEXT NOT NULL,
    before_image_path TEXT,
    after_image_path TEXT,
    delta_qpf_image_path TEXT,
    change_ptype_image_path TEXT,
    edit_mask_image_path TEXT,
    review_image_path TEXT,
    created_by TEXT,
    created_at TEXT NOT NULL
);
```

### 16.4 edit_operation

```sql
CREATE TABLE edit_operation (
    operation_id TEXT PRIMARY KEY,
    version_id TEXT NOT NULL,
    window_id TEXT NOT NULL,
    tool_name TEXT NOT NULL,
    variable_name TEXT NOT NULL,
    operation_type TEXT NOT NULL,
    parameters_json TEXT,
    mask_path TEXT,
    before_stats_json TEXT,
    after_stats_json TEXT,
    created_by TEXT,
    created_at TEXT NOT NULL
);
```

### 16.5 review_field

```sql
CREATE TABLE review_field (
    field_id TEXT PRIMARY KEY,
    window_id TEXT NOT NULL,
    version_id TEXT,
    source_model TEXT NOT NULL,
    variable_name TEXT NOT NULL,
    level_type TEXT,
    level_value INTEGER,
    lead_hour INTEGER,
    valid_time TEXT,
    unit TEXT,
    file_path TEXT NOT NULL,
    created_at TEXT NOT NULL
);
```

### 16.6 review_product

```sql
CREATE TABLE review_product (
    review_id TEXT PRIMARY KEY,
    window_id TEXT NOT NULL,
    version_id TEXT NOT NULL,
    template_id TEXT NOT NULL,
    image_path TEXT NOT NULL,
    plot_config_path TEXT,
    plot_input_manifest_path TEXT,
    plot_code_version TEXT,
    created_at TEXT NOT NULL
);
```

---

## 17. 后端 API 设计

### 17.1 查询产品窗口

```http
GET /api/windows?case_id=2026010108&accum_hours=24
```

返回：

```json
{
  "windows": [
    {
      "window_id": "2026010108_ACC24_024_048",
      "case_id": "2026010108",
      "accum_hours": 24,
      "start_lead": 24,
      "end_lead": 48,
      "status": "available"
    }
  ]
}
```

### 17.2 加载窗口数据

```http
GET /api/window/{window_id}/load
```

返回当前 qpf、ptype 的数据摘要和图片路径。

### 17.3 编辑预览

```http
POST /api/edit/preview
```

请求：

```json
{
  "window_id": "2026010108_ACC24_024_048",
  "version_id": "draft",
  "tool": "polygon",
  "variable": "qpf",
  "operation": "increase",
  "mask": {
    "type": "polygon",
    "coordinates": []
  },
  "parameters": {
    "delta_mm": 3.0,
    "only_nonzero": true
  }
}
```

返回：

```json
{
  "affected_grid_count": 286,
  "affected_area_km2": 7150,
  "before_stats": {
    "min": 0.2,
    "max": 8.5,
    "mean": 3.1,
    "sum": 886.6
  },
  "after_stats": {
    "min": 3.2,
    "max": 11.5,
    "mean": 6.1,
    "sum": 1744.6
  },
  "preview_image": "images/preview.png"
}
```

### 17.4 应用编辑

```http
POST /api/edit/apply
```

作用：把预览结果写入当前草稿，并记录一条操作日志。

### 17.5 撤销 / 重做

```http
POST /api/edit/undo
POST /api/edit/redo
```

### 17.6 保存版本

```http
POST /api/version/save
```

请求：

```json
{
  "window_id": "2026010108_ACC24_024_048",
  "user": "forecaster_001",
  "generate_review": true
}
```

返回：

```json
{
  "version_id": "v003",
  "before_image": "images/before_product.png",
  "after_image": "images/after_product.png",
  "review_image": "images/review_composite.png"
}
```

### 17.7 生成复盘图

```http
POST /api/review/generate
```

请求：

```json
{
  "window_id": "2026010108_ACC24_024_048",
  "version_id": "v003",
  "template_id": "snow_phase_review_v1"
}
```

---

## 18. 技术路线建议

### 18.1 推荐架构

```text
前端：Vue / React + Canvas 或 OpenLayers
后端：Python FastAPI
格点处理：NumPy / xarray / rasterio / shapely
数据格式：NetCDF / NPZ / PNG / JSONL / CSV
数据库：PostgreSQL，原型阶段可用 SQLite
文件存储：本地文件系统，后续可扩展 MinIO
绘图服务：Python 绘图代码适配器
```

### 18.2 不建议路线

不建议按 GFIE 的老式重客户端方式直接复刻。原因是：

1. 本项目区域固定、底图固定，Web/轻量前端即可满足。
2. 本项目核心是数据留痕和复盘归档，Web 后端更易做版本、日志、批量任务。
3. 后续接入绘图代码和测试数据，Python 服务化更方便。

---

## 19. 绘图代码接入方案

### 19.1 基础出图

已有出图代码用于生成：

```text
before_product.png
after_product.png
delta_qpf.png
change_ptype.png
edit_mask.png
```

建议封装为统一接口：

```python
def plot_product(
    qpf_path: str,
    ptype_path: str,
    metadata: dict,
    output_path: str
) -> str:
    ...
```

### 19.2 复盘组合图

后续提供的复盘绘图代码建议封装为：

```python
def plot_review_composite(
    plot_input_manifest_path: str,
    output_path: str,
    config_path: str | None = None
) -> str:
    ...
```

或者：

```python
def plot_review_composite(payload: dict) -> str:
    ...
```

### 19.3 绘图代码版本记录

每次绘图应记录：

```text
绘图代码名称
绘图代码版本
绘图配置文件
绘图输入 manifest
绘图开始时间
绘图结束时间
绘图是否成功
错误日志
```

---

## 20. 统计与复盘指标

### 20.1 降水订正统计

```text
修改格点数
修改面积
订正前最小值、最大值、平均值、总量
订正后最小值、最大值、平均值、总量
总量增加量
总量减少量
最大单格点增量
最大单格点减量
```

### 20.2 相态变化矩阵

| 订正前 | 订正后 | 格点数 |
|---|---:|---:|
| 雨 | 雪 | 52 |
| 雨 | 雨夹雪 | 31 |
| 雨夹雪 | 雪 | 39 |
| 雪 | 雨夹雪 | 8 |
| 雪 | 雨 | 2 |

### 20.3 操作行为统计

```text
多边形操作次数
绘制笔操作次数
笔刷操作次数
筛查清零次数
降水修改次数
相态修改次数
累计影响格点数
累计影响面积
最终保存版本号
```

---

## 21. 权限与安全

首版建议设置三类角色：

| 角色 | 权限 |
|---|---|
| 预报员 | 编辑、预览、保存草稿、生成复盘 |
| 审核员 | 查看、审核、保存正式版本、导出复盘包 |
| 管理员 | 用户管理、配置管理、模板管理、数据清理 |

关键约束：

1. 原始版本不可修改。
2. 保存版本不可覆盖，只能新增版本。
3. 删除复盘包需要管理员权限。
4. 每条操作必须记录用户和时间。
5. 出图失败不得影响格点保存，但应记录失败状态和日志。

---

## 22. 实施阶段

### 阶段一：MVP 编辑闭环

目标：完成核心编辑和保存。

内容：

```text
1. 固定底图加载
2. 5km 网格数据加载
3. 24h/48h/168h 窗口选择
4. qpf/ptype 显示
5. 多边形选区
6. 笔刷选区
7. 降水定值、增加、减少、乘系数、清零
8. 相态改雨、雨夹雪、雪、无降水
9. 弱降水筛查清零
10. 撤销/重做
11. 保存版本
12. 保存操作日志
13. 基础 before/after 出图
```

### 阶段二：复盘绘图接入

目标：接入 IFS 多要素复盘变量和绘图代码。

内容：

```text
1. 复盘变量 manifest 设计
2. IFS 变量切片读取
3. 当前窗口 + 前后24h 数据组织
4. plot_input_manifest.json 生成
5. 调用复盘绘图代码
6. 保存 review_composite.png
7. 保存绘图日志
```

### 阶段三：复盘数据包与模板管理

目标：形成稳定复盘产品。

内容：

```text
1. 复盘数据包导出
2. 复盘图模板配置
3. 批量复盘图生成
4. 统计表输出
5. 多版本对比
6. 操作行为统计
```

### 阶段四：增强功能

可选增强：

```text
1. 区域移动
2. 区域复制
3. 圆形工具
4. 等值线辅助显示
5. 降水量级快捷按钮
6. 审核流
7. 与实况检验对接
```

---

## 23. 复杂度评估

| 模块 | 复杂度 | 说明 |
|---|---:|---|
| 固定底图显示 | 低 | 已有底图和绘图代码 |
| 5km 格点加载 | 中 | 需统一网格、变量、维度 |
| 累计时段选择 | 低到中 | 只支持 24/48/168，复杂度可控 |
| 多边形/笔刷编辑 | 中 | 关键在像素到格点映射和 mask 生成 |
| 降水编辑 | 中 | 算法简单，但需保证统计和日志完整 |
| 相态编辑 | 中 | 需处理 qpf 与 ptype 一致性 |
| 版本保存 | 中 | 必须不可覆盖、可追溯 |
| 基础出图 | 低到中 | 依赖现有绘图代码适配 |
| IFS 复盘变量归档 | 中高 | 变量多、层次多、时效需对齐 |
| 复盘组合绘图 | 中高 | 依赖后续绘图代码和测试数据 |
| 批量复盘分析 | 中高 | 可作为后期扩展 |

总体判断：

> 首版编辑闭环复杂度中等；复盘系统复杂度主要取决于 IFS 变量种类、数据格式和绘图代码输入要求。

---

## 24. 风险与应对

| 风险 | 表现 | 应对 |
|---|---|---|
| 网格与底图映射不准 | 鼠标选区落不到正确格点 | 建立 grid_definition.json，并做格点拾取测试 |
| 只保存图片不保存格点 | 后续无法复盘和统计 | 强制保存 qpf/ptype before/after 和 delta/mask |
| 累计窗口命名混乱 | 24h/48h/168h 数据混淆 | 统一 window_id 命名规则 |
| qpf 与 ptype 不一致 | 0 降水仍显示雪 | 设置 qpf_threshold 并自动清理相态 |
| 绘图代码输入不统一 | 复盘图生成失败 | 建立 plot_input_manifest.json 作为适配层 |
| IFS 变量过多导致存储膨胀 | 复盘包过大 | 只保存本次绘图用到的变量切片，同时记录原始路径 |
| 后续测试数据格式不确定 | 接入延期 | 先做数据适配器接口，等测试数据到位后落地 |
| 操作误修改 | 难以回退 | 每次操作保存历史，支持撤销/重做 |
| 版本覆盖 | 原始数据丢失 | 原始版本不可变，保存版本只新增不覆盖 |

---

## 25. 验收标准

### 25.1 功能验收

| 编号 | 验收项 | 标准 |
|---|---|---|
| A01 | 产品窗口选择 | 可选择 24h/48h/168h 和起始时效 |
| A02 | 数据加载 | 可加载 qpf_before 和 ptype_before |
| A03 | 地图显示 | 可显示固定底图和当前降水/相态结果 |
| A04 | 多边形编辑 | 可圈选区域并修改 qpf 或 ptype |
| A05 | 笔刷编辑 | 可小范围修改 qpf 或 ptype |
| A06 | 筛查清零 | 可将小于阈值的降水清零并同步相态为无降水 |
| A07 | 撤销重做 | 可撤销和恢复最近编辑操作 |
| A08 | 保存版本 | 可生成不可覆盖的新版本 |
| A09 | 差异生成 | 可生成 delta_qpf、change_ptype、edit_mask |
| A10 | 基础出图 | 可生成 before、after、diff、change 图 |
| A11 | 复盘绘图 | 可调用绘图代码生成复盘组合图 |
| A12 | 数据归档 | 可导出完整复盘数据包 |

### 25.2 数据验收

1. 每个 window_id 下必须有 qpf_before、ptype_before、qpf_after、ptype_after。
2. 每个保存版本必须有 delta_qpf、change_ptype、edit_mask。
3. 每次操作必须有 operation_log 记录。
4. 每张复盘图必须有 plot_input_manifest。
5. 原始数据不可被覆盖。

### 25.3 性能验收

首版建议性能目标：

| 操作 | 目标 |
|---|---:|
| 加载单个窗口 | 5 秒内 |
| 多边形预览 | 3 秒内 |
| 笔刷响应 | 1 秒内 |
| 保存版本 | 10 秒内，不含复杂复盘图 |
| 基础出图 | 10 秒内，具体取决于绘图代码 |
| 复盘组合图生成 | 以绘图代码实际耗时为准，建议记录任务进度 |

---

## 26. 配置文件建议

### 26.1 grid_definition.json

```json
{
  "grid_id": "region_5km_v1",
  "nx": 420,
  "ny": 360,
  "resolution_km": 5,
  "projection": "to_be_defined",
  "lon_min": 105.0,
  "lon_max": 125.0,
  "lat_min": 25.0,
  "lat_max": 45.0,
  "png_width": 1600,
  "png_height": 1200,
  "row_col_order": "y_desc_x_asc"
}
```

### 26.2 product_config.json

```json
{
  "supported_accum_hours": [24, 48, 168],
  "qpf_threshold": 0.1,
  "ptype_mapping": {
    "0": "无降水",
    "1": "雨",
    "2": "雨夹雪",
    "3": "雪"
  }
}
```

### 26.3 plot_config.json

```json
{
  "default_templates": [
    "precip_phase_compare_v1",
    "window_prev_next_v1",
    "snow_phase_review_v1"
  ],
  "review_time_policy": {
    "current_window": ["middle", "end"],
    "prev24_window": ["end"],
    "next24_window": ["start"]
  }
}
```

---

## 27. 推荐首版功能清单

### 27.1 必须做

```text
1. 24h / 48h / 168h 累计长度选择
2. 起始时效选择
3. 固定底图显示
4. qpf + ptype 当前结果显示
5. 多边形选区
6. 绘制笔选区
7. 笔刷选区
8. 降水定值、增加、减少、乘系数、清零
9. 相态改雨、雨夹雪、雪、无降水
10. 弱降水筛查清零
11. 撤销/重做
12. 保存版本
13. 保存 qpf_before、ptype_before、qpf_after、ptype_after
14. 保存 delta_qpf、change_ptype、edit_mask
15. 保存操作日志
16. 调用基础绘图代码生成 before、after、diff、change 图
17. 生成当前时段 + 前后24h 的复盘绘图输入
18. 调用复盘绘图代码生成 review_composite 图
```

### 27.2 可以二期做

```text
1. 区域移动
2. 区域复制
3. 圆形选区
4. 等值线辅助显示
5. 降水量级快捷按钮
6. 多版本对比
7. 批量导出复盘包
8. 绘图模板管理
9. 复盘图批量生成
```

### 27.3 不建议做

```text
1. 背景场选择
2. 多模式切换
3. 850/700/500 等 IFS 要素前端交互浏览
4. 3h/6h 分段拆分
5. 修改累计量后回推分段原始数据
6. 格点趋势调整
7. 要素协同编辑
8. 站点修改
9. AI 自动订正
```

---

## 28. 最终结论

本系统应建设为一个轻量但数据留痕严格的网格编辑与复盘系统。核心原则是：

> 主编辑界面只处理累计降水和相态；IFS 多要素诊断场不进入编辑流程，只进入复盘绘图和归档流程。

首版最重要的闭环是：

```text
选择累计窗口
→ 显示累计降水和相态
→ 区域化修改量级或相态
→ 预览和应用
→ 保存订正前后格点
→ 生成差异和修改掩膜
→ 记录操作日志
→ 调用绘图代码
→ 生成并归档复盘数据包
```

这样既能满足当前业务需要，又不会把系统做成过重的综合气象平台。后续在你提供具体绘图代码和测试数据后，重点工作应放在：

```text
1. 明确 IFS 复盘变量清单
2. 明确变量文件格式和时效规则
3. 定义 plot_input_manifest.json
4. 封装绘图代码适配器
5. 打通保存版本后自动生成复盘图的流程
```

---

## 附录 A：首版交互流程图

```text
[选择起报时间]
        ↓
[选择累计长度：24/48/168]
        ↓
[选择起始时效]
        ↓
[加载 qpf_before / ptype_before]
        ↓
[显示原始结果图]
        ↓
[多边形 / 绘制笔 / 笔刷选区]
        ↓
[选择降水调整或相态调整]
        ↓
[预览]
        ↓
[应用]
        ↓
[记录操作日志]
        ↓
[保存版本]
        ↓
[生成 qpf_after / ptype_after / delta / mask]
        ↓
[基础出图]
        ↓
[组织 IFS 复盘变量]
        ↓
[调用复盘绘图代码]
        ↓
[归档复盘数据包]
```

---

## 附录 B：首版最小数据包

```text
minimum_review_package/
    metadata.json
    qpf_before.nc
    ptype_before.nc
    qpf_after.nc
    ptype_after.nc
    delta_qpf.nc
    change_ptype.nc
    edit_mask.nc
    operation_log.jsonl
    before_product.png
    after_product.png
    delta_qpf.png
    change_ptype.png
    review_composite.png
    plot_input_manifest.json
```

---

## 附录 C：未来增强方向

1. 接入实况检验，分析人工订正是否提升预报质量。
2. 对历史个例建立订正样本库。
3. 统计不同区域、时段、天气型下的常见订正行为。
4. 研究 EC IFS 在雨雪相态临界区的系统性偏差。
5. 以人工订正样本训练后续自动订正模型。
6. 扩展复盘模板，支持更多高空场、剖面图、过程图组合。

