## 20. 统计与复盘指标

### 20.1 降水订正统计

```text
修改格点数
修改面积
订正前最小值、最大值、平均值、总量
订正后最小值、最大值、平均值、总量
总量增加量
总量减少量
最大单格点增量
最大单格点减量
```

### 20.2 相态变化矩阵

矩阵必须覆盖所有 4 种编码（0/1/2/3）之间的双向变化，包括清零（→0）和新增落区（0→）。

| 订正前 | 编码 | 订正后 | 编码 | 格点数 | 场景 |
|---|---:|---|---:|---:|---|
| 雨 | 1 | 无降水 | 0 | 120 | 弱降水清零 |
| 雪 | 2 | 无降水 | 0 | 45 | 弱降水清零 |
| 雨夹雪 | 3 | 无降水 | 0 | 18 | 弱降水清零 |
| 无降水 | 0 | 雨 | 1 | 86 | 补充降水落区 |
| 无降水 | 0 | 雪 | 2 | 32 | 补充降水落区 |
| 无降水 | 0 | 雨夹雪 | 3 | 5 | 补充降水落区 |
| 雨 | 1 | 雪 | 2 | 52 | 相态调整 |
| 雨 | 1 | 雨夹雪 | 3 | 31 | 相态调整 |
| 雨夹雪 | 3 | 雪 | 2 | 39 | 相态调整 |
| 雪 | 2 | 雨夹雪 | 3 | 8 | 相态调整 |
| 雪 | 2 | 雨 | 1 | 2 | 相态调整 |
| 雨夹雪 | 3 | 雨 | 1 | 0 | 相态调整 |

> **注意**：降水清零（qpf 设为 0）时 ptype 必须同步设为 0；新增降水落区（qpf 从 0 变为正值）时 ptype 必须同步设为 1/2/3。此一致性规则在 §12 中定义。

### 20.3 操作行为统计

```text
多边形操作次数
绘制笔操作次数
笔刷操作次数
筛查清零次数
降水修改次数
相态修改次数
累计影响格点数
累计影响面积
最终保存版本号
```

---

## 21. 权限与安全

首版建议设置三类角色：

| 角色 | 权限 |
|---|---|
| 预报员 | 编辑、预览、保存草稿、生成复盘 |
| 审核员 | 查看、审核、保存正式版本、导出复盘包 |
| 管理员 | 用户管理、配置管理、模板管理、数据清理 |

关键约束：

1. 原始版本不可修改。
2. 保存版本不可覆盖，只能新增版本。
3. 删除复盘包需要管理员权限。
4. 每条操作必须记录用户和时间。
5. 出图失败不得影响格点保存，但应记录失败状态和日志。

---

## 22. 实施阶段

### 阶段一：MVP 编辑闭环

目标：完成核心编辑和保存。

内容：

```text
1. 固定底图加载
2. 0.05° 网格数据加载
3. 24h/48h/168h 窗口选择
4. qpf/ptype 显示
5. 多边形选区
6. 笔刷选区
7. 降水定值、增加、减少、乘系数、清零
8. 相态改雨、雨夹雪、雪、无降水
9. 弱降水筛查清零
10. 撤销/重做
11. 保存版本
12. 保存操作日志
13. 基础 before/after 出图
```

### 阶段二：复盘绘图接入

目标：接入 IFS 多要素复盘变量和绘图代码。

内容：

```text
1. 复盘变量 manifest 设计
2. IFS 变量切片读取
3. 当前窗口 + 前后24h 数据组织
4. plot_input_manifest.json 生成
5. 调用复盘绘图代码
6. 保存 review_composite.png
7. 保存绘图日志
```

### 阶段三：复盘数据包与模板管理

目标：形成稳定复盘产品。

内容：

```text
1. 复盘数据包导出
2. 复盘图模板配置
3. 批量复盘图生成
4. 统计表输出
5. 多版本对比
6. 操作行为统计
```

### 阶段四：增强功能

可选增强：

```text
1. 区域移动
2. 区域复制
3. 圆形工具
4. 等值线辅助显示
5. 降水量级快捷按钮
6. 审核流
7. 与实况检验对接
```

---

## 23. 复杂度评估

| 模块 | 复杂度 | 说明 |
|---|---:|---|
| 固定底图显示 | 低 | 已有底图和绘图代码 |
| 0.05° 格点加载 | 中 | 需统一网格、变量、维度 |
| 累计时段选择 | 低到中 | 只支持 24/48/168，复杂度可控 |
| 多边形/笔刷编辑 | 中 | 关键在像素到格点映射和 mask 生成 |
| 降水编辑 | 中 | 算法简单，但需保证统计和日志完整 |
| 相态编辑 | 中 | 需处理 qpf 与 ptype 一致性 |
| 版本保存 | 中 | 必须不可覆盖、可追溯 |
| 基础出图 | 低到中 | 依赖现有绘图代码适配 |
| IFS 复盘变量归档 | 中高 | 变量多、层次多、时效需对齐 |
| 复盘组合绘图 | 中高 | 依赖后续绘图代码和测试数据 |
| 批量复盘分析 | 中高 | 可作为后期扩展 |

总体判断：

> 首版编辑闭环复杂度中等；复盘系统复杂度主要取决于 IFS 变量种类、数据格式和绘图代码输入要求。

---

## 24. 风险与应对

| 风险 | 表现 | 应对 |
|---|---|---|
| 网格与底图映射不准 | 鼠标选区落不到正确格点 | 建立 grid_definition.json，并做格点拾取测试 |
| 只保存图片不保存格点 | 后续无法复盘和统计 | 强制保存 qpf/ptype before/after 和 delta/mask |
| 累计窗口命名混乱 | 24h/48h/168h 数据混淆 | 统一 window_id 命名规则 |
| qpf 与 ptype 不一致 | 0 降水仍显示雪 | 设置 qpf_threshold 并自动清理相态 |
| 绘图代码输入不统一 | 复盘图生成失败 | 建立 plot_input_manifest.json 作为适配层 |
| IFS 变量过多导致存储膨胀 | 复盘包过大 | 只保存本次绘图用到的变量切片，同时记录原始路径 |
| 后续测试数据格式不确定 | 接入延期 | 先做数据适配器接口，等测试数据到位后落地 |
| 操作误修改 | 难以回退 | 每次操作保存历史，支持撤销/重做 |
| 版本覆盖 | 原始数据丢失 | 原始版本不可变，保存版本只新增不覆盖 |

---

## 25. 验收标准

### 25.1 功能验收

| 编号 | 验收项 | 标准 |
|---|---|---|
| A01 | 产品窗口选择 | 可选择 24h/48h/168h 和起始时效 |
| A02 | 数据加载 | 可加载 qpf_before 和 ptype_before |
| A03 | 地图显示 | 可显示固定底图和当前降水/相态结果 |
| A04 | 多边形编辑 | 可圈选区域并修改 qpf 或 ptype |
| A05 | 笔刷编辑 | 可小范围修改 qpf 或 ptype |
| A06 | 筛查清零 | 可将小于阈值的降水清零并同步相态为无降水 |
| A07 | 撤销重做 | 可撤销和恢复最近编辑操作 |
| A08 | 保存版本 | 可生成不可覆盖的新版本 |
| A09 | 差异生成 | 可生成 delta_qpf、change_ptype、edit_mask |
| A10 | 基础出图 | 可生成 before、after、diff、change 图 |
| A11 | 复盘绘图 | 可调用绘图代码生成复盘组合图 |
| A12 | 数据归档 | 可导出完整复盘数据包 |

### 25.2 数据验收

1. 每个 window_id 下必须有 qpf_before、ptype_before、qpf_after、ptype_after。
2. 每个保存版本必须有 delta_qpf、change_ptype、edit_mask。
3. 每次操作必须有 operation_log 记录。
4. 每张复盘图必须有 plot_input_manifest。
5. 原始数据不可被覆盖。

### 25.3 性能验收

首版建议性能目标：

| 操作 | 目标 |
|---|---:|
| 加载单个窗口 | 5 秒内 |
| 多边形预览 | 3 秒内 |
| 笔刷响应 | 1 秒内 |
| 保存版本 | 10 秒内，不含复杂复盘图 |
| 基础出图 | 10 秒内，具体取决于绘图代码 |
| 复盘组合图生成 | 以绘图代码实际耗时为准，建议记录任务进度 |

---

## 26. 配置文件建议

### 26.1 grid_definition.json

```json
{
  "grid_id": "region_005deg_v1",
  "business_name": "5km 加密格点",
  "nx": 821,
  "ny": 501,
  "dlon": 0.05,
  "dlat": 0.05,
  "projection": "EPSG:4326",
  "lon_min": 70.0,
  "lon_max": 111.0,
  "lat_min": 25.0,
  "lat_max": 50.0,
  "png_width": 1600,
  "png_height": 1200,
  "row_col_order": "y_desc_x_asc",
  "data_format": "csv_comma_separated",
  "area_mode": "approximate_25km2",
  "note": "0.05° 等经纬度网格，纬向约 5.5km，经向随纬度变化（25°N≈5.0km，50°N≈3.6km）。area_mode 可选 approximate_25km2（首版近似）或 spherical_lonlat_cell_area（逐格精算）"
}
```

### 26.2 product_config.json

```json
{
  "supported_accum_hours": [24, 48, 168],
  "qpf_threshold": 0.1,
  "init_hours": [8, 20],
  "time_step_hours": 3,
  "max_lead_hours": 240,
  "ptype_mapping": {
    "0": { "label": "无降水", "color_fn": "transparent" },
    "1": { "label": "雨", "color_fn": "rain_gradient" },
    "2": { "label": "雪", "color_fn": "snow_gradient" },
    "3": { "label": "雨夹雪", "color_fn": "rain_snow_fixed" }
  },
  "rain_color_scale": [
    { "min": 0.1, "max": 10, "rgb": [170, 240, 144], "label": "小雨" },
    { "min": 10, "max": 25, "rgb": [61, 172, 5], "label": "中雨" },
    { "min": 25, "max": 50, "rgb": [104, 185, 249], "label": "大雨" },
    { "min": 50, "max": 100, "rgb": [5, 4, 247], "label": "暴雨" },
    { "min": 100, "max": 250, "rgb": [253, 4, 255], "label": "大暴雨" },
    { "min": 250, "max": 9999, "rgb": [253, 4, 255], "label": "特大暴雨" }
  ],
  "snow_color_scale": [
    { "min": 0.1, "max": 2.5, "rgb": [209, 210, 211], "label": "小雪" },
    { "min": 2.5, "max": 5, "rgb": [166, 167, 168], "label": "中雪" },
    { "min": 5, "max": 10, "rgb": [117, 118, 119], "label": "大雪" },
    { "min": 10, "max": 9999, "rgb": [77, 78, 79], "label": "暴雪" }
  ],
  "rain_snow_color": [251, 201, 252],
  "data_files": {
    "precipitation": "tp_999_deeplearning_YYYYMMDDHH_hhh.txt",
    "probability": "pr_999_deeplearning_YYYYMMDDHH_hhh.txt",
    "phase": "ptype_999_revised_YYYYMMDDHH_hhh.txt"
  }
}
```

### 26.3 plot_config.json

```json
{
  "default_templates": [
    "precip_phase_compare_v1",
    "window_prev_next_v1",
    "snow_phase_review_v1"
  ],
  "review_time_policy": {
    "current_window": ["middle", "end"],
    "prev24_window": ["end"],
    "next24_window": ["start"]
  }
}
```

---

## 27. 推荐首版功能清单

### 27.1 必须做

```text
1. 24h / 48h / 168h 累计长度选择
2. 起始时效选择
3. 固定底图显示
4. qpf + ptype 当前结果显示
5. 多边形选区
6. 绘制笔选区
7. 笔刷选区
8. 降水定值、增加、减少、乘系数、清零
9. 相态改雨、雨夹雪、雪、无降水
10. 弱降水筛查清零
11. 撤销/重做
12. 保存版本
13. 保存 qpf_before、ptype_before、qpf_after、ptype_after
14. 保存 delta_qpf、change_ptype、edit_mask
15. 保存操作日志
16. 调用基础绘图代码生成 before、after、diff、change 图
17. 生成当前时段 + 前后24h 的复盘绘图输入
18. 调用复盘绘图代码生成 review_composite 图
```

### 27.2 可以二期做

```text
1. 区域移动
2. 区域复制
3. 圆形选区
4. 等值线辅助显示
5. 降水量级快捷按钮
6. 多版本对比
7. 批量导出复盘包
8. 绘图模板管理
9. 复盘图批量生成
```

### 27.3 不建议做

```text
1. 背景场选择
2. 多模式切换
3. 850/700/500 等 IFS 要素前端交互浏览
4. 3h/6h 分段拆分
5. 修改累计量后回推分段原始数据
6. 格点趋势调整
7. 要素协同编辑
8. 站点修改
9. AI 自动订正
```

---

## 28. 最终结论

本系统应建设为一个轻量但数据留痕严格的网格编辑与复盘系统。核心原则是：

> 主编辑界面只处理累计降水和相态；IFS 多要素诊断场不进入编辑流程，只进入复盘绘图和归档流程。

首版最重要的闭环是：

```text
选择累计窗口
→ 显示累计降水和相态
→ 区域化修改量级或相态
→ 预览和应用
→ 保存订正前后格点
→ 生成差异和修改掩膜
→ 记录操作日志
→ 调用绘图代码
→ 生成并归档复盘数据包
```

这样既能满足当前业务需要，又不会把系统做成过重的综合气象平台。后续在你提供具体绘图代码和测试数据后，重点工作应放在：

```text
1. 明确 IFS 复盘变量清单
2. 明确变量文件格式和时效规则
3. 定义 plot_input_manifest.json
4. 封装绘图代码适配器
5. 打通保存版本后自动生成复盘图的流程
```

---

## 附录 A：首版交互流程图

```text
[选择起报时间]
        ↓
[选择累计长度：24/48/168]
        ↓
[选择起始时效]
        ↓
[加载 qpf_before / ptype_before]
        ↓
[显示原始结果图]
        ↓
[多边形 / 绘制笔 / 笔刷选区]
        ↓
[选择降水调整或相态调整]
        ↓
[预览]
        ↓
[应用]
        ↓
[记录操作日志]
        ↓
[保存版本]
        ↓
[生成 qpf_after / ptype_after / delta / mask]
        ↓
[基础出图]
        ↓
[组织 IFS 复盘变量]
        ↓
[调用复盘绘图代码]
        ↓
[归档复盘数据包]
```

---

## 附录 B：首版最小数据包

```text
minimum_review_package/
    metadata.json
    qpf_before.nc
    ptype_before.nc
    qpf_after.nc
    ptype_after.nc
    delta_qpf.nc
    change_ptype.nc
    edit_mask.nc
    operation_log.jsonl
    before_product.png
    after_product.png
    delta_qpf.png
    change_ptype.png
    review_composite.png
    plot_input_manifest.json
```

---

## 附录 C：未来增强方向

1. 接入实况检验，分析人工订正是否提升预报质量。
2. 对历史个例建立订正样本库。
3. 统计不同区域、时段、天气型下的常见订正行为。
4. 研究 EC IFS 在雨雪相态临界区的系统性偏差。
5. 以人工订正样本训练后续自动订正模型。
6. 扩展复盘模板，支持更多高空场、剖面图、过程图组合。

---

## 附录 D：Docker 部署方案

### D.1 开发流程

```text
本地开发 → 本地测试 → 打镜像 → 部署到生产内网
```

开发阶段前后端分别独立运行：

```text
前端：npm run dev（Vite 开发服务器，热更新）
后端：uvicorn main:app --reload（FastAPI 开发模式）
```

### D.2 生产部署结构

```text
docker-compose.yml
├── frontend（Nginx 容器）
│   ├── Vue 构建产物
│   └── nginx.conf（反向代理 /api → backend）
├── backend（FastAPI 容器）
│   ├── Python 3.11 + NumPy/xarray/Matplotlib/Cartopy
│   ├── 绘图字体（simhei.ttf）
│   └── Cartopy 地图数据（离线）
└── volumes
    ├── data/（格点数据挂载）
    ├── archive/（归档目录挂载）
    └── db/（SQLite 数据库文件挂载）
```

### D.3 关键注意事项

1. Matplotlib + Cartopy 镜像较大（约 1-2 GB），建议使用多阶段构建减小体积。
2. 生产内网无外网访问，Cartopy 地图数据和字体必须离线打入镜像。
3. 格点数据和归档目录通过 Docker Volume 挂载，不打入镜像。
4. 前端构建产物为静态文件，由 Nginx 容器直接托管，同时反向代理 API 请求到后端容器。

---

## 附录 E：参考项目技术要点摘要

### E.1 yy-webgis-bj（前端参考）

可直接复用的组件和模式：

| 组件/模式 | 位置 | 对本项目的价值 |
|---|---|---|
| OlMap 地图封装 | src/components/OlMap/ | 地图初始化、投影配置、底图管理 |
| GridLayer | src/layers/gridLayer.js | 静态网格图像叠加（imageExtent [70,25,111,50]） |
| GirdValueWebGLTileLayer | src/components/OlMap/layers/ | WebGL 动态网格色斑图渲染 |
| 投影管理 Store | src/stores/modules/projection.ts | EPSG:4326 + proj4 注册 |
| 时间轴 Store | src/stores/modules/timeline.ts | 时效选择和播放控制 |
| 图层管理 Store | src/stores/modules/layerManager.ts | 图层增删、透明度、可见性 |
| EventBus | src/utils/eventBus.js | 跨组件事件通信 |

### E.2 要素场绘图（绘图参考）

已有绘图脚本直接作为复盘绘图服务的核心代码，接入时需要：

1. 将 meteva GDS 数据读取替换为本地 NumPy/xarray 读取。
2. 将 Flask 图片服务替换为 FastAPI 异步任务调用。
3. 保留 PlateCarree 投影、色标配置、边界绘制逻辑。
4. 新建降水/相态编辑前后对比图绘制函数（复用 rain.py 色标）。

### E.3 cdsz（后端参考）

可借鉴的设计：

| 功能 | 位置 | 对本项目的参考价值 |
|---|---|---|
| GRIB 文件解析 | com/util/GribFileReaderUtil.java | EC GRIB 数据读取逻辑 |
| 双线性插值 | com/util/BilinearInterpolateUtil.java | 格点数据插值方法 |
| 数据入库策略模式 | com/station/indb/strategy/ | 多数据源适配器设计 |
| JWT 认证 | com/authorize/ | 用户认证流程 |
| 操作日志 | com/log/ | 日志记录和查询 |

