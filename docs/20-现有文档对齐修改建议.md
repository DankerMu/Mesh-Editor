# 20. 现有文档对齐修改建议

## 20.1 目标

现有文档已经补充完整业务平台能力，但不同文件之间存在目标层级不一致的问题。本文件给出具体修改建议，使 `docs/01-09` 与新增 `docs/10-19` 对齐。

---

## 20.2 `建设方案.md` 修改建议

### 20.2.1 修改版本号

当前：

```text
版本：V1.1
```

建议：

```text
版本：V2.0
说明：完整业务平台方案 + 工程开发设计索引
```

### 20.2.2 修改文档索引

当前索引只到 `docs/09`。建议追加 `docs/10-20`，内容见本包 README。

### 20.2.3 修改概述措辞

当前概述已是完整业务平台，可保留。建议明确：

```text
01-09 为业务方案与功能设计；10-20 为工程开发设计。
```

---

## 20.3 `docs/02-架构与流程.md` 修改建议

### 20.3.1 扩展架构总览

建议将原架构改成：

```text
数据接入层
├── DataScanService
├── QpfBuilder
├── PtypeBuilder
└── product_window 状态管理

编辑工作台层
├── EditorView
├── PrecipPhaseGridLayer
├── DrawTools
└── editorStore

编辑引擎层
├── MaskBuilder
├── EditOps
├── Consistency
└── StatsCalc

版本审核发布层
├── VersionService
├── ApprovalService
├── ReleaseService
└── release_product

复盘绘图层
├── ReviewService
├── PlotTaskService
├── plotters
└── review_product

复盘中心与历史分析层
├── ReviewCenterView
├── StatsService
└── export

权限审计配置层
├── AuthService
├── AuditService
├── ConfigService
└── TemplateService

运维监控层
├── health
├── storage monitor
├── task monitor
└── scan log
```

### 20.3.2 增加成品流程

新增：

```text
数据接入流程
编辑流程
审核发布流程
复盘绘图流程
历史统计流程
配置变更流程
```

并引用 `docs/10-开发总蓝图.md`。

---

## 20.4 `docs/03-数据与编码.md` 修改建议

该文件已补充 ptype_before 合成规则和 start_lead=000 fallback，整体较完整。建议补充：

```text
1. pr 当前不进入编辑，但文件扫描可统计 pr_files_found；
2. ptype 合成结果应保存 ptype_source_manifest.json，记录参与合成的 ptype leads、缺失 leads、qpf_threshold；
3. qpf_before 建议保存 qpf_build_manifest.json，记录 start/end 文件路径、negative_count、missing_count。
```

---

## 20.5 `docs/04-前端设计.md` 修改建议

当前文件偏编辑页。建议增加“完整前端模块”章节：

```text
一级菜单：网格编辑 / 版本审核 / 复盘中心 / 历史分析 / 系统管理
```

并增加：

```text
前端路由详见 docs/12
前端 Store 详见 docs/12
PrecipPhaseGridLayer 实现详见 docs/12
```

---

## 20.6 `docs/05-编辑功能.md` 修改建议

保留业务说明，但增加引用：

```text
编辑算法、mask 生成、preview/apply、undo/redo 的工程实现详见 docs/14。
```

建议把“恢复原始相态”定义清楚：

```text
恢复原始相态 = ptype_after[mask] = ptype_before[mask]，但仍受 qpf_threshold 一致性约束。
```

---

## 20.7 `docs/06-版本与复盘.md` 修改建议

建议补：

```text
1. review_payload 的详细生成逻辑详见 docs/15；
2. 复盘包导出格式详见 docs/15；
3. 状态机详见 docs/16；
4. API Schema 详见 docs/17。
```

并新增：

```text
ptype_source_manifest.json
qpf_build_manifest.json
```

---

## 20.8 `docs/07-数据库与API.md` 修改建议

当前文档已经很长且包含完整 API。建议不要继续塞 Schema 细节，而是拆分职责：

```text
07：数据库表和 API 业务说明
16：状态机和错误码
17：API Schema 和权限审计契约
```

建议在 `docs/07` 开头增加：

```text
本文件定义数据库表和 API 业务语义；具体请求/响应 Schema、统一错误码、权限和审计策略见 docs/17。
```

另外建议补索引和约束章节：

```text
数据库索引、唯一约束和迁移策略见 docs/11。
```

---

## 20.9 `docs/08-技术与绘图.md` 修改建议

当前已写绘图三层架构。建议补：

```text
绘图任务队列、复盘 payload、missing_fields、partial_success、复盘包导出详见 docs/15。
```

同时将“已有脚本不可直接服务化调用”的内容保留，这是重要开发约束。

---

## 20.10 `docs/09-实施与验收.md` 修改建议

### 20.10.1 删除 MVP 表述

当前仍有：

```text
阶段一：MVP 编辑闭环
```

建议替换为：

```text
阶段一：工程骨架与配置/权限基础
阶段二：数据接入与窗口生成
阶段三：编辑工作台与编辑引擎
阶段四：版本审核发布
阶段五：复盘中心与绘图任务队列
阶段六：历史统计、监控运维与试运行
```

详细任务迁移到 `docs/19`。

### 20.10.2 权限角色对齐

当前“首版建议三类角色”应改为四类：

```text
admin / reviewer / forecaster / viewer
```

与 `docs/07` 的 user 表和权限矩阵对齐。

### 20.10.3 验收标准扩展

增加：

```text
审核发布验收
权限审计验收
复盘中心验收
绘图任务队列验收
配置管理验收
监控运维验收
历史统计验收
```

---

## 20.11 新增文档之间的引用关系

```text
10 开发总蓝图
├── 引用 11 后端工程设计
├── 引用 12 前端工程设计
├── 引用 13 数据适配
├── 引用 14 编辑引擎
├── 引用 15 绘图复盘
├── 引用 16 状态机错误码
├── 引用 17 API Schema
├── 引用 18 测试设计
└── 引用 19 任务拆解
```

开发时优先阅读顺序：

```text
10 → 16 → 11/12 → 13/14/15 → 17 → 18 → 19
```

---

## 20.12 最终文档体系建议

```text
业务方案层：
01 背景与目标
02 架构与流程
03 数据与编码
04 前端设计
05 编辑功能
06 版本与复盘
07 数据库与API
08 技术与绘图
09 实施与验收

工程开发层：
10 开发总蓝图
11 后端工程设计
12 前端工程设计
13 数据适配与文件IO设计
14 编辑引擎详细设计
15 绘图任务与复盘服务设计
16 状态机与错误码设计
17 API契约与Schema设计
18 测试用例与验收数据设计
19 开发任务拆解与里程碑
20 现有文档对齐修改建议
```

---

## 20.13 合并检查清单

```text
[ ] 主文档版本号已改为 V2.0
[ ] 主文档索引已加入 docs/10-20
[ ] docs/02 架构已扩展为完整业务平台架构
[ ] docs/04 已指向 docs/12 前端工程设计
[ ] docs/05 已指向 docs/14 编辑引擎设计
[ ] docs/06 已指向 docs/15 复盘绘图设计
[ ] docs/07 已指向 docs/16/17 状态机和 API Schema
[ ] docs/08 已指向 docs/15 绘图任务设计
[ ] docs/09 已删除 MVP 表述并按成品里程碑重写
[ ] 角色定义统一为 admin/reviewer/forecaster/viewer
[ ] ProductWindow/EditSession/EditVersion/PlotTask 状态命名全仓一致
[ ] 错误码在前后端共享
```
